---
title: uinify
description: 一个小型 Chat UI 基础库，将后端协议处理留在 UI 层之外。
---

欢迎使用 uinify，这是一个面向 Chat UI 的 React 库，负责把传输层和渲染层分开。

你可以把后端事件映射成 `UiStreamEvent`，派发到 runtime，再渲染对话内容。

## 快速开始

```bash
npm install uinify react react-dom
```

阅读 [快速开始](/zh-cn/getting-started/) 搭建第一个 Chat UI。
