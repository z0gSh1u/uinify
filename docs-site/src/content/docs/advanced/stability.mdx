---
title: Stability Guide
description: Which surfaces are stable for adoption and which are reference-only.
---

This page separates the surfaces intended for adoption from the surfaces that are useful for learning from the current implementation. Read it after [Getting Started](/getting-started/) and [Core Concepts](/guides/core-concepts/) so the stable surface here has practical context.

## Recommended Surface

Prefer building integrations on the narrowest stable surface that solves the problem:

- Core runtime entry points such as `createChatRuntime`, canonical `UiStreamEvent` mapping, `UiMessagePart`, `UiStepPart`, `UiImagePart`, and `UiComposerCommand`.
- `createAdapterRunner` from the package root as the default stable adapter entry point.
- React transcript entry points such as `ChatRoot`, `MessageList`, `Message`, `MessagePart`, `StepBlock`, and `ImagePart`.
- Renderer override seams such as `renderStep`, `renderImage`, `renderReasoning`, and artifact registry hooks.
- Composer entry points such as `LexicalComposer`, structured command selections, and host-owned attachment submit values.
- Styling hooks in `uinify/styles.css`, stable `data-slot` regions, and `slotClassNames`.

This is the recommended surface because it matches the public exports and the docs-backed integration patterns.

`createAdapterResult` and `validateAdapterEvents` are part of the public package root, but they are lower-level helpers for custom adapter wrappers rather than the default adoption path.

## Stable Styling Hooks

For styling and theming, prefer the documented hooks in this order:

- CSS custom properties under the `--uinify-*` namespace in `uinify/styles.css`
- Stable `data-slot` regions such as `message`, `message-parts`, `message-actions`, `part-actions`, `step`, `step-header`, `step-category`, `step-status`, `image`, `artifact-container`, `artifact-tabs`, `artifact-views`, `attachment-tray`, `attachment-actions`, and `attachment-part`
- `slotClassNames` keys that map directly to those documented stable regions

Prefer tokens first, then stable slots and `slotClassNames`, and replace renderers only when the default structure is the wrong product shape rather than merely the wrong visual treatment.

Commands are stable as composer input intent. Steps are stable as transcript output. Do not document command selections as tool execution results, and do not document tools as a separate canonical tool call transcript part.

## Reference-only Surface

Some code in the repository is intentionally useful as a reference, but it should not be treated as a stable integration contract.

- Files under `examples/adapters/` are reference-only examples.
- Example adapters such as `custom-minimal`, `agent-like`, and `openai-like` show how to use the stable adapter helpers, not supported compatibility layers by themselves.
- Internal implementation details behind the current renderer shells and examples may evolve as the package adds more official docs and extension points.

Use the reference-only surface to understand patterns, naming, and trade-offs. Do not couple production integrations to those files as if they were versioned APIs.
